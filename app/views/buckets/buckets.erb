<h2><%= @user.username %>'s buckets</h2>
<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

  <% @buckets.each do |bucket| %>
    <a href="buckets/<%= bucket.id %>"><%= bucket.name %></a></br>
  <% end %>


<a class="button" href="/buckets/new">New Bucket</a>
