

<div class="burn_row">
  <div class="first"><a href="/buckets/<%= bucket.id %>/edit"><%= bucket.name %></a></div>
  <div><%= bucket.sum %><% if bucket.size %> of <%= bucket.size %><% end %> <% if bucket.unit %><%= bucket.unit.name.pluralize(bucket.size) %><%end%></div>

</div>
