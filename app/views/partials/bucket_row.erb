

<div class="burn_row">
  <div class="first"><%= bucket.name %></div>
  <div><% if bucket.unit %><%= bucket.sum %><%end%><% if bucket.size %> of <%= bucket.size %><% end %> <% if bucket.unit %><%= bucket.unit.name.pluralize(bucket.size) %><%end%></div>

</div>
